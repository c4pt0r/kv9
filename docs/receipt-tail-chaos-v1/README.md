# Receipt-tail full21 Chaos evidence

Exact clean runtime: `a6ac335ef4567f2e1a2b33da1a723d694cd5b7d9`.
`summary.json` is derived only after the actual runtime and all six post phases pass. It retains the three complete original histories, all outcomes including unknown/refused calls, four fresh final drains, exact source/build/image identities, scoped cleanup and the complete local archive identity. This adds no throughput, latency, promotion, cross-host or physical power-loss claim.

Run `python3 verify.py` here for independent full portable-byte readback and invocation/return/outcome population checks. Original semantic history-checker reports and their source helpers are retained in the archive. This reader does not rerun live cluster/source/ELF/WAL acceptance. It creates no extracted paths. Symlinks are represented as literal metadata.

Original ELF/WAL payloads, the full local data archive and documented duplicate independent-audit copies remain local. `publication-preparation/omitted-archive-members.json` records every omitted file's original size/SHA and, only where recorded size/hash identity proves it, its duplicate counterpart. The complete archive inventory/result remain packaged. No original bytes are deleted or rewritten by publication.

The prospective portable-v3 policy is separate from runtime: a fresh baseline after all post phases, 8 GiB continuous available-space floor, at most 1 GiB sampled decrease, and 8 MiB extra launch metadata reserve (9 GiB + 8 MiB launch). Bounds remain 512 MiB aggregate decoded members, 128 MiB/member, 512 MiB compressed, 2 MiB parts, and 1200 seconds; at most 10,000 members. No payload restore occurs. The accepted publisher lineage, prior selector omission/failure and explicit narrow changes are retained. Both actual receipt recovery terminal paths are included.
