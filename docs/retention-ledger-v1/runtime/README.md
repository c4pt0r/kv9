# Retention-ledger environment and runtime evidence

This finite packet preserves the completed local ledger Chaos cell and the environment changes needed to run it. `selection.json` maps every portable member to its original path, byte count and SHA-256. `package-result.json` binds the gzip archive and bounded parts; `readback-result.json` records full gzip EOF and member-by-member comparison with the original bytes. Concatenate parts in numeric order to reconstruct `evidence.tar.gz`; its first member is the exact selection manifest. No originals were moved or removed for publication.

The actual default-feature binary is `2b4d70aac139672a5627e5a0f789245ea039b647f2510aec6f6b938551b1d36e`. Runtime session 96465 ended at tool receipt d5c80a/0. Independent serial Raw KV audit 268089/0 checked all 296 calls, including one real overwrite, deletes and recovery reads. Independent ledger audit 6d7cff/0 checked 16 calls: eight changed transactions, two duplicate confirmations and six exact owner reads. The old agreed leader (voter 3) died with exit 137 through actual PodChaos while source and destination ownership overlapped. The restarted process retained its Pod/PVC/store identities. Destination publication, source quiescence/release and application through all three voters completed. All 526 command lifetimes exited successfully and were absent at audit; the owned namespace was removed, with eight historical namespace UIDs and the old fault preserved.

The packet includes both complete client histories and command request/response evidence, the actual 147,730-byte selected recovery SST, and all three complete 18,114,560-byte stopped-node tar archives. Each inner archive includes its original Raft log and local store files, with its original full-readback inventory. Duplicate payload command stdout is omitted because the named payload files are included. The registered earlier checkpoint SST descriptor (143,108 bytes, `aeb165c2ba15f38013c6563b58c4dd7170c2280c135a6435f4bd0222e5c4a909`) differs from the captured later recovery SST (`1e3908d4a9b5c0fc45ac296980b85ba018e622aae64877da2257b18cdafc0217`). The earlier object's body was not separately retained before scoped MinIO cleanup; no claim of preserving that body is made.

The storage guard retains the 8 GiB floor on each distinct filesystem, 9 GiB plus 8 MiB launch requirement, and 1 GiB aggregate conservative decrease. It deduplicates a shared filesystem and retains peak decrease after external cleanup. Ten deterministic guard controls passed at 6a622d/0. The executed helper additionally contains root's explicit tool-path overrides and ledger hooks; launch preflight binds its exact bytes and confirms the tested budget logic is unchanged. Across 1,530 runtime samples, host and output peak decreases summed to 587,988,992 bytes. These historical runtime measurements exclude this later reporting packet's allocation, which is recorded separately.

The original full-library attempt remains failed (207 passed, six failed, one ignored). All six exact failures subsequently passed once using the same frozen test ELF, original four test threads and unchanged assertions/timeouts, with fresh fixture data on the original `/tmp` filesystem and logs on `/mnt/data`. Session 96013 ended at 6d85ae/0. This discriminator does not establish a full-suite pass or prove filesystem placement was the sole cause: concurrent large tool extraction and subset scheduling also differed. Exact original status/metrics, failed log, source/ELF pins, new results and the timing caveat are included. A reporting-only permission failure remains separate.

Capacity assessment failures and the external capacity change are retained. The agent created `/mnt/data/kv9-work` and changed future output guidance; it did not cause the externally observed increase in free space, delete the missing historical tool/client paths, or move historical evidence. Preservation of `/tmp/kv9-point-write-v3-release-first/native/kv9-batch-benchmark` remains unverified. That missing fixed performance client remains a causal-input limitation for later performance work. Kind was restored to the repository-pinned version; the existing cluster was read, not recreated.

Credentials, private kubeconfig, CA bytes, image layers and executables are excluded. Their nonsecret source/version/content pins and lineage remain where appropriate; credentials themselves are neither hashed nor archived. Selection exclusions also identify unrelated broad Docker inventory and old unit-fixture payloads. This is one single-host container death and checkpoint recovery with bounded tmpfs MinIO, not object-store power-loss durability, full fault-matrix, physical garbage-collection, general concurrent-history, backfill or performance acceptance.

Recheck the portable package without original files with `python3 verify.py`; add `--originals` on the original host for byte-for-byte comparison. The original verification used helper CPUs 6–15 and 22–31. The verifier writes a fresh result; preserve an existing result before any separate later verification in a new copy.
