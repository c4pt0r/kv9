This controller covers only frozen ordinals 1–95. Cohort 000 is a mandatory completed prerequisite: actual finish 56949/ba03d0/0 and independent final audit 88896/109415/0 are pinned in cohort-zero-binding.json. The original 96-cohort preparation, every selection, and every original catalog remain unchanged.

Run the metadata/mocked-I/O controls, then use ROOT-COMMANDS.json with this preparation's independently reviewed inventory hash. Root has authorized the exact deterministic per-cohort release conditions; there is no per-cohort approval prompt. Each iteration runs the original stage-verify mode, independently checks exact resident source/base/patch identities and digests, the full raw/old-compressed verification proof, retained toolchain sources/copies, six precise codec roles and all intent/start/final/embedded receipts, then durably records the actual verification SHA and supplies it to the original finish mode. Finish still performs original retirement, exact-path restore, whole-cohort selected reader, and the second COLD transition.

Only one controller owns campaign.lock. The inherited dispatcher still owns its original serial.lock during each phase; the controller takes that same lock for independent release validation, then releases it before invoking the unchanged dispatcher. The dispatcher revalidates inputs under its own lock before mutation. Root must serialize unrelated builds, workloads, codecs and manual migrations. No child is adopted or restarted from a live or unknown handle.

A restart can skip only a contiguous prefix of this controller's complete.json records whose current COLD state, release, restoration, reader, preserved identities, patch identities and child outcomes still match. Transaction and dispatcher ordinal sets must equal bootstrap 000 plus that prefix. Any partial/failed/unknown ordinal stops automatic progress and requires root reconciliation using the original per-cohort restore/resume commands. Nothing erases failed attempts, partial patches or original evidence. Inode/ctime differences after restoration remain explicit.

Fresh available space is checked before every new cohort and every five seconds while a child runs. The inherited 8 GiB floor and all per-cohort caps remain. A separate 256 MiB controller output/metadata reservation is added to the unchanged transaction and original dispatcher reservations. Event history is capped at 64 MiB; each stdout/stderr is capped at 1 MiB; stage-verify has a 4,500-second outer bound, finish 9,000 seconds, and one controller invocation 48 hours. All inner 1,200/1,800/2,100-second limits remain unchanged. Parent-death SIGKILL and owned process-group termination are installed; descendant uncertainty after interruption prevents automatic continuation and is retained for root inspection.

status.json is an atomically replaced progress pointer; every earlier observation remains in events.jsonl. Existing transaction accounting is reused and separately charges this controller's preparation and outputs. Group accounting explicitly includes completed bootstrap 000; it never credits it twice as new campaign recovery. Stop occurs only at a completed cohort boundary when actual available space reaches 100,000,000,000 bytes, or at finite exhaustion. Exhaustion can be complete with target_reached=false. No state claims benchmark readiness or launches a benchmark.

All 96 exact restore-final/readback-final and partial rollback commands remain in the unchanged predecessor ROOT-COMMANDS.json. Standard and retained-encoder dependencies, adapted reader labels, full historical predicates, failed outcomes and original payload scope are preserved. This preparation has executed no codec or migration.
