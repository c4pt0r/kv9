# Published-directory actual Chaos Mesh evidence

Exact runtime: `483b8c3629b033734f5d7a2b8653a1352304a4b5`.
The complete 21-window baseline, independent full-history audit, full local
archive/readback and exact-owned cleanup pass. `summary.json` records actual
operation/outcome counts, source/binary identities, drains and exited lifetimes.
Run `python3 verify.py` to independently decode and hash-check every portable
member without extraction. Original executable/WAL payloads and duplicate audit
copies remain local with complete original hash references; this reporting
package alone cannot replay every full-file audit.

The baseline does not establish positive engine segment-rotation coverage.
A separate threshold-crossing and leader-crash supplement remains required.
There is no new throughput, latency, physical power-loss or cross-host result.
CRC main stays selected; no original industrial work-package checkbox closes.

The portable-v2 reporting policy requires 65 GiB available at launch, a 64 GiB
floor and at most 1 GiB sampled decrease. It changes no runtime or benchmark
policy, retained payload or archive verification bound. The original publisher
and explicit differences are retained. CI ran locally; no hosted job was dispatched.
