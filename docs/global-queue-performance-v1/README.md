# Global queue polling experiment evidence

This bundle preserves 1,092 original files (218,147,737 decoded bytes) from
source qualification, release provenance, ordinary recovery, the complete
performance screen, independent acceptance/statistics, and storage preparation.
Six compressed parts total 11,160,319 bytes.

Run `python3 docs/global-queue-performance-v1/verify.py` to verify every part,
member and indexed top-level file without extraction. This verifies integrity;
it does not replace correctness, runtime acceptance or statistical analysis.

The [performance report](../GLOBAL-QUEUE-PERFORMANCE.md) rejects the candidate;
the [validation report](../GLOBAL-QUEUE-VALIDATION.md) states its completed and
remaining correctness scopes. Original terminal receipts: source 20559/0
`b7b3a0`, release 13411/0 `8b87ad`, recovery 85740/0 `7c855f`, recovery audit
`f8fc8a`/0, smoke 87007/0 `697788`, timing 86527/0 `33a452`, independent audit
39011/0 `3d9a7c`, and statistics `c54b18`/0. All 12 smoke and 24 timed cohorts
are retained. No failed or partial runtime is substituted or pooled.

Original executable artifacts, full WAL and bulky host observations remain
local under the paths bound by the included original inventories. They are
not uploaded in this reporting selection. Candidate source is `3338650`;
selected runtime remains `ca0002c7`. Timed builds have default features.

This shared-host, loopback, tmpfs-WAL comparison does not establish equal Redis
durability, real-disk or cross-host performance, candidate Chaos acceptance,
full implementation proofs, sustained capacity or industrial readiness.
