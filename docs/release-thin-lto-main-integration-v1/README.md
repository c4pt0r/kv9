# ThinLTO main integration reporting evidence

This bundle records main integration `11113f68f6a5df77da1ffb4fcec850953716ffa3`, source qualification, both original build attempts, and the later exact-source ordinary streaming/unary process recovery and independent full-history audit. It preserves original source paths, decoded sizes/SHA hashes, stdout/errors, manifests, codegen/source maps, complete histories/reports, and root terminal receipts.

The 148 runtime/build paths match qualified ThinLTO02d. Main-source checks cover 709 default workspace tests/doctests with 23 existing ignored and 438 observer checks with one existing ignored; these overlapping configurations are not summed. The actual observer-feature Clippy invocation is recorded. The first build completed but lacked verbose rustc command evidence, so its readback failed and remains separate. The corrected original build records explicit ThinLTO/codegen-unit1/opt3/default-feature portable unwind codegen and new11113 source manifests. Its server and native workload bytes reproduce the prior qualified02d artifacts; that does not relabel their manifests or invent a new performance/Chaos run.

The ordinary recovery includes both complete point/batch atomic histories:291 operations,262OK and29unknown, with all outcomes retained; explicit process/lifetime identities, failure/restart windows, six fresh voter drains, and owned cleanup. The unchanged independent audit is original evidence; the archive verifier does not rerun it. The new main-source confirmation and old exact-candidate709/recovery/full21/full72 evidence remain distinct.

`inventory.json` maps archived files to original paths and exact decoded hashes, and lists compressed parts of at most2MiB. `python3 verify.py` checks this reporting bundle without extraction. WAL/data, compressed objects, binaries, caches, old archive payloads, GitHub API bodies and credentials are omitted before payload reads/hashing. Original inventories remain the authority for omitted byte hashes. This is not a payload backup, a portable full-runtime re-audit, new benchmark evidence, or a new Chaos acceptance result.

The six exact `fixture/data/n{1,2,3}/{metrics.json,status}` metadata files are explicit exceptions to data-subtree exclusion. They retain default26 metric and exporter/process status evidence, each bound to the original audit input inventory. All other files under data remain excluded without traversal.
