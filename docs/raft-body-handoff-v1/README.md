# Request-body handoff evidence

This compact bundle supports [the report](../RAFT-BODY-HANDOFF-RESULTS.md) for
source `3c3bd9b981e00a66ef5b18dbbabfc973da65b878`. It retains 320 original files,
21,248,693 decoded bytes, in one 1,012,106-byte archive part. Display aliases
[classes.csv](classes.csv), [readout.json](readout.json) and the original
[analysis.json](analysis.json) retain exact derivation/accepted-analysis bytes.

All 48 class rows, 336 outcomes, compositions, endpoint identities/offsets and
session events remain visible. This is an instrumented two-cell diagnostic,
with 984,862 successful measured calls; it is not a new performance selection.
Offer-to-receiver-yield includes admission and is not wire delivery or ACK.
Independent populations cannot be added into request latency. Unknown drops
never produce a duration. Sequential snapshots are not an atomic ledger.

The archive includes original source-gate failure and follow-up, release/cache
receipts, preparation/validator contracts, actual stream/unary process histories,
two-cell configurations/reports/endpoint metrics and accepted independent audits.
Original source-first runtime tests pass; the only follow-up source change
removes an unused test-only helper for Clippy. The release binds the corrected
clean source. All original files remain local at inventory paths.

The publication is a compact selection, distinct from the runtime auditor's
78 files / 497,469,741 bytes. Raw WAL, executables and bulky host observations
remain local under original inventories. This is not a standalone full runtime
re-audit, Chaos qualification or a complete implementation proof.

Run `python3 verify.py` in this directory to check exact part/member/alias bytes
and safe archive paths without extraction. Integrity verification does not rerun
tests, the workload, derivation or audit. No hosted CI was dispatched.
