# Packet completion

The finite metadata archive contains **1,213 exact members**, **12,641,592 file bytes** and **13,977,600 decoded tar bytes**. Compressed size is **1,709,578 bytes**.

Archive SHA-256: `2c696f7eee38f9da62e05c4e822bd23519bbaac9b8551f75f110269f33bf7d45`.

Packaging passed actual **261edb / exit 0**. The separate verifier passed actual **03d347 / exit 0**, reading every member and the complete gzip stream through EOF once. It extracted no files and read no original payload. The exact per-member paths, lengths and SHA values are in `archive-manifest.json`; the original path map and historical capacity values are in `source-selection.json` and `summary.json`.

All preparation, packaging and verification activity is terminal. `publication-terminals.json` preserves actual tool results and the initial reporting-only failure. This packet is metadata evidence, not new runtime acceptance or a reservation of free space. Root's subsequent builds remain independent. Reporting allocation is separately recorded in the final inventory; all original stage records and accounting remain unchanged.
