# Event interval one experiment evidence

This bundle supports [the report](../RPC-EVENT-ONE-PERFORMANCE.md) for clean
candidate `5bed688c3b829770092ee6802078102b894ef6d5`. The candidate regresses
throughput, read means and tails; selected CRC remains unchanged.

The archive retains 1,019 original files / 234,683,157 decoded bytes, compressed
to 13,951,001 bytes in seven bounded parts. It includes source/build/cache gates,
ordinary stream/unary recovery histories, frozen preparations and contracts,
all 24 cohort configurations/reports, endpoint observations, independent audit,
statistics, root terminal receipts and the scoped Go compiler-cache reclamation.
First read-only census and root statistics-wrapper failures are retained.

[READOUT.md](READOUT.md) corrects only the generated document's inherited title.
[READOUT.original.md](READOUT.original.md) retains the original derivation bytes;
[PER-REPEAT.md](PER-REPEAT.md) is unchanged. Every role, source revision, dataset,
comparison and result belongs to this event-one experiment. The arithmetic core
and accepted audit remain unchanged; no workload is rerun for a display fix.

The first complete timing/audit accepts 48,550,288 successful measured calls in
one attempt, 80 exited lifetimes, 48 drains/bindings and 4,679 resource samples.
All phases preserve 48,848,344 calls / 48,848,360 attempts. Ordinary recovery
checks 372 calls, 338 OK and 34 unknown without blind replay.

The original runtime inventory's 636 files / 4,447,964,001 bytes has a different
scope. Raw WALs, binaries and bulky host observations remain local at original
inventory paths. This compact selection is not a standalone full runtime
re-audit, whole-implementation proof, full-72 acceptance or Chaos qualification.

Run `python3 verify.py` here to verify part/member/alias identity and safe archive
paths without extraction. It checks integrity only; it does not execute tests,
the workload, statistics or the audit. No hosted CI was dispatched.
