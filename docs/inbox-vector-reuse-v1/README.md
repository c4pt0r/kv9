# Inbox vector reuse experiment evidence

This bundle supports [the report](../INBOX-VECTOR-REUSE-PERFORMANCE.md) for
candidate `1045755eac4292a2ccfdfa3d71741ed60e2f4668`. It contains 966 original
files / 210,116,900 decoded bytes in five parts totaling 10,095,862 bytes.
The [readout](READOUT.md) and [per-repeat table](PER-REPEAT.md) preserve the
original statistics output bytes.

The first complete 24-cohort timing attempt, independent audit and statistics
all pass. All 49,860,618 measured calls succeed in one attempt. No partial or
failed cohort is pooled, resumed or dropped. Before runtime, root reclaims only
rebuildable pip HTTP downloads following a reference/link census; original
source/release identities, evidence and all storage guards remain unchanged.

Original source checks, release/cache provenance, complete recovery histories,
seven driver/source-binding and 17 auditor contracts, smoke receipts, frozen
harnesses, raw timing reports/histograms, audit inventories, statistics and root
terminal records are retained. Recovery covers 359 operations: 330 OK and
29 unknown, with five server and two client lifetimes exited and six drains.
Root timing 75925 exits 0 (`5f0ade`); audit 4861 exits 0 (`e95fa4`); statistics
exits 0 (`2951e1`). No hosted CI runs.

The independent timing audit's 636 files / 4,573,041,910 bytes have a different
scope from this compact publication. WALs, retained binaries and bulky host
observations remain local under the original inventories. This selection cannot
perform a standalone full runtime re-audit. Ordinary recovery is not actual
Chaos Mesh or physical-durability acceptance, and the source representation
argument is not a whole-implementation proof. CRC remains selected.

Run `python3 verify.py` here to check bounded archive parts, safe paths, exact
member bytes and top-level aliases. This is integrity verification only; it does
not rerun tests, recovery, timing, independent acceptance or statistics.
