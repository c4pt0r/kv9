# Current CRC main CPU diagnostic preparation

This is an **unexecuted** derivative of the accepted active-prefix CPU diagnostic published at `4d9af2af4b6d5629afd3105ab1afab05ce1591a0:docs/POST-CRC-CPU-PROFILE.md`. It binds clean **bd42e60f84657e22e36e34924a5c80a08eac623a**, source `/tmp/kv9-crc-main-bd42-baseline-20260914-first`, original release `/tmp/kv9-crc-main-integration-release-20260914-first`, server SHA `106f517c84fabe790ea139f3c18661e14447abd9e257fdd6ba82bfabe6796eb9`, manifest SHA `9005c064129e4a70b9de881e46074c4b7b5ec493e482095dd6fa7884fccdfb4a`. The release's accepted original readback binds all 859 source files, default feature rows and actual ThinLTO link. This preparation read metadata and clean Git HEAD/status; new runtime must still rehash its actual full source/binary inputs.

The client remains exact `0be806d9671e2c50701a64aa7889c8859b7648ba`, source `/tmp/kv9-point-write-measurement-v3`, native release `/tmp/kv9-point-write-v3-release-first/native`, binary SHA `8da9af469f962a938027d1970141bbe4622f7d42b2b795f720e288fb3f8d5957`. The new release's workload executable is not substituted for this fixed measurement client.

**Finite source change:** all 27 matched-driver functions are byte-identical; only its old-role revision/server/manifest constants change. `profile.py` changes the local driver path/hash, server source/build paths and protocol ID to `kv9-crc-main-bd42-write-active-prefix-cpu-v1`. `supervise.py` changes only ROOT. `analyze.py`, `active_prefix.py`, `test_prefix_contract.py` and `qualify_cleanup.py` are byte-identical. Original source copies, exact diffs, hashes and static AST/reverse-diff checks are retained. No imported reader or runtime predicate is weakened.

The original ca0002c7 byte-table results remain historical. Its reported frame CRC/FNV percentages and disassembly offsets are **not** current slicing-by-eight evidence. New sample attribution requires this exact bd42 recording, unchanged independent decode, and (if needed later) new disassembly against the actual retained current binary. No old instruction range is reused.

## Preserved protocol and gates

Two sequential fresh three-voter fixtures: c64 point PUT and BatchPut(64), 4,096 keys plus sentinel, 128-byte values, seed 71, 128 warmups, five-second measurement, 10M call cap, 1,500ms SDK deadline and the original six configured maximum attempts. Clients use CPUs 0–1; voters 2–5; profiler/prefix/decoder helpers 6–15,22–31. Normal quorum, sync calls and commit/apply acknowledgments remain; WAL is volatile tmpfs with full post-exit uncompressed retention. This is CPU attribution on a shared host, not QPS acceptance, power-loss durability, or a new background-container isolation protocol.

Each original 20-second recording uses only three owned voter PIDs, `cpu-clock` at 199Hz, DWARF 16KiB, mono clock, max-size 128MiB (strictly below cap at acceptance), no build-id cache and no BPF events. After profiler observation and the existing 250ms delay, 128 paced absent-key unary RawGet calls over one second run with at most eight concurrent CLI children, 1,500ms per-call/four-second successful-prefix bounds, 64KiB per-output bound and no retry. Two fresh empty publications follow the prefix; less than 12 seconds of recorder time may have elapsed before client launch. All failures remain and abort launch/acceptance.

The unchanged decoder requires nominal start/end containment, excludes 1ms at each measurement edge, requires clock-offset spread below 1ms, more than 1,000 selected samples, identical periods, all 32 aggregate bins and selected samples within 50ms of both retained edges. Owned PID scope, full raw identity, zero loss, exact workload/dataset/retention/cleanup predicates remain. A busy prefix or live perf alone does not establish coverage. CPU fractions cannot be called end-to-end waiting time; stacks overlap, optimized/async unwinding is incomplete, and current unstripped symbol/stack availability remains empirical.

## Reuse and pending execution

Original seven pure controls passed and two real Python dummy-process failure cleanup cases passed; helper bytes are unchanged. Their exact first receipts and expected failed prefix outcomes are bound by `inherited-controls.json`. Actual predecessor runtime was 76156/38b8e5/0 and decoder 21261/e10616/0. These qualify the reused mechanism, not new bd42 sample coverage. Earlier missing-prefix failure and discovery failures remain hash-bound/preserved. No controls, database, profiler, decoder, build, codec or acceptance run occurred here.

`commands.json` gives exact root-only arrays. Root first performs the fresh checks in `preflight.requirements.json` and creates actual `preflight-first.json`. That pending requirements file is not a passing receipt. Then run `supervise.py profile`; only after its actual successful root/child terminal run `supervise.py analyze`. All outputs are fresh beside these helpers; no old cohort or original perf data is copied into them. Root retains actual invocation/tool terminal and source-before/after records. No automatic retry or publication is prepared.

The local backend resolves to `/usr/lib/linux-hwe-6.17-tools-6.17.0-19/perf`; its file is present. Historical expected tool SHA/version are bound; current payload/version/owned-PID permission must be checked during root preflight/runtime. Observed `perf_event_paranoid=4`, `kptr_restrict=1`: existing sudo-owned-PID recording is required; no host settings change. The unchanged helper requires the source-bound v3 scripts, RESP helper, current bd42 source/release/build metadata and fixed client source/release. Independent decode additionally requires the **new** original `perf.data`, raw hash/clock/PID records, reports/configs, retained data and cleanup/priming receipts. Published old report bundles omit raw perf/WAL/ELFs and cannot replace these local artifacts; both old raw perf paths currently exist but were not opened.

## Capacity scope

Original profiles retained 673,916,837 point bytes and 4,034,471,657 batch bytes of data (4,708,388,494 combined), plus 113,184,668 raw perf bytes. A proportional fresh estimate reserves the historical data, **both full 128MiB recorder caps**, and **1GiB for reporting/allocation variation** above the unchanged 96GiB retained-disk preflight: **109,129,780,878 available bytes**. The metadata observation was 121,114,202,112 disk and 66,274,996,224 tmpfs bytes. This is neither full physical census nor guaranteed output bound; faster current writes can require more. Existing 32GiB tmpfs preflight and 16/64GiB tmpfs/disk runtime floors remain. The original tmpfs snapshot's additional 8GiB lower guard and copy/compare-before-scratch-removal are unchanged. Root must reobserve capacity and activity immediately before release; runtime readiness remains false.
