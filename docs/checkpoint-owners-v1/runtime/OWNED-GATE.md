The deterministic pre-upload cut needs a separate `checkpoint-testing` executable. It is not exercised by the default helper in this preparation.

Use the exact same frozen production source and default Cargo graph plus only root/server `checkpoint-testing`. Retain the actual build command, Cargo feature records, source before/after inventory and executable hash separately. Do not use the default binary or pass a pause environment variable to claim a cut that default code compiles out.

The smallest useful cut is `owned`, in `CheckpointWorker::resume_pending`: `CheckpointOwners::before_io` has returned after exact applied Pending acquisition/publication, while `PendingFlush::recover` has not yet started PUT/GET. `planned` is earlier (v2 journal durable but owners may not exist); `prepared` is later (remote I/O already completed). The gate is selected by `KV9_TESTING_FLUSH_PAUSE_DIR=/data/owner-gates`, then `/data/owner-gates/N-owned`. The worker creates `/data/owner-gates/N-owned.arrived` containing `paused` and waits until its gate is removed. The `.arrived` marker itself is an observation, not a durable recovery authority.

A separate narrowly adapted feature cell should:

1. Retain the entire base Raw sequence, physical 16 MiB rotation/reclamation, selected remote base, three-voter health and resources. Set only the testing environment in the new owned Deployments. Wait for an absent pending slot and stable current owner/manifest state before arming the gate on the agreed leader. Do not infer quiescence from a missing sample.
2. Arm only that leader's `owned` gate. Submit one explicitly labelled additional Raw write to trigger a new data revision; keep the original Raw history and label the added call. Wait for actual `.arrived=paused` and the same agreed leader. If leadership changes before the cut, preserve failure; do not re-arm another process automatically.
3. Capture exact `/data/catalog.pending` bytes before the kill (bound to `prepare_remote` in the examined source), checksum and v2 payload inventory. V2 is `KV9PENDING\x02`, little-endian u64 predecessor, little-endian u32 manifest length, canonical manifest, exact object bodies in manifest order, then SHA256 of the complete preceding body. Enforce original 48 MiB checkpoint + 1 MiB descriptor + 128-byte journal limit, exact sizes/hashes and no trailing bytes. Derive the two owner IDs from that actual journal's manifest and predecessor, independently of topology generation. Observe Pending Published and Version absent through real CLI. Capture each planned object body now, even if the object has not been uploaded.
4. For at least one newly content-addressed object, collect a typed MinIO NoSuchKey response at the armed cut. A generic nonzero `mc` exit, permission failure or existing identical object does not establish pre-upload absence. Record the actual pause location and external observations separately; no fabricated upload trace. Other content-addressed objects may already exist legitimately.
5. Inject one actual PodChaos container-kill targeting that exact old leader/container. Require actual AllInjected, target UID and old exit 137. Before unblocking the replacement, preserve the same Pod/PVC/store identity and the unchanged v2 journal bytes while its same gate remains armed. Remove only the exact owned gate after the new process is identified. This needs a small post-injection/pre-Serving hook in the base cell or an independently retained feature cell; the current default wrapper has no such hook. Do not claim the feature cell is executable yet.
6. Observe the exact original IDs after restart. If the original planned publication succeeds, require Version Published and Pending Released, exact acknowledged PUT+GET object bytes, v2-to-v1 progression/settlement and journal clearance, followed by the unchanged fresh all-voter write/readback. If leader loss causes a legitimate competing manifest predecessor, retain the typed negative settlement and pinned Pending; it is not a successful handoff. Do not force a release or weaken this predicate to make the cell pass. A deterministic successful handoff after this cut may need a separately justified scheduling gate on the new leader; this is an explicit open design input, not current acceptance.
7. Retain the complete original and additional Raw history, all owner observations, both journal captures, object bodies, worker logs, all three stopped-store/Raft archives, actual fault/lifecycle commands and scoped cleanup. Same 1200-second deadline, each distinct filesystem's 8 GiB floor, 9 GiB + 8 MiB launch, aggregate 1 GiB peak decrease, original payload/output caps. Journal and extra objects count against that same budget; no baseline reset.

The default helper remains useful immediately once its new default binary and source pins are supplied: exact selected automatic ownership and remote-base recovery after actual leader death. It honestly reports `live_acquired_phase_observed=false` and `pre_upload_crash_exercised=false`. A Released observation is evidence of the accepted ledger terminal state, not a separately observed transient phase.
