The new default checkpoint worker passed one actual leader container-kill and remote-checkpoint recovery with automatic ownership. Runtime 23271/32b9ce/0 and independent audit 94289/2c5d7a/0 are retained with their original invocations and outcomes. No workload was replayed for reporting.

The original bounded Raw sequence produced 286 successful operations: 275 puts, two deletes, eight reads and one keyspace creation. Physical 16 MiB rotation required 270 60-KiB filler writes under the unchanged 300-write cap. The earlier 296-call result is historical, not a forced count. Keep, delete, overwrite and fresh post-rejoin values passed; all voters reached the final barrier. The selected leader's old container exited 137, the new process recovered generation 72/cut 1487/publication 1496, and Pod/PVC/store identity stayed equal. Scoped cleanup removed the owned namespace and preserved eight historical namespace UIDs plus the old fault.

Independent byte derivation binds the actual root, exact selected manifest and predecessor 71 to Pending/Version IDs. Four real CLI reads see Pending Released and Version Published before/after the fault. Full committed replay of all three retained Raft logs independently finds Pending Held/Published/Quiesced/Released at indexes 1493/1495/1502/1504, Version Held/Published at 1498/1500, and the exact manifest publication at 1496. This is automatic worker ownership; the fixture submits zero retention mutations. It does not claim that a transient acquired state was observed live via CLI.

All three original stopped-store archives and the exact selected remote SST body are included. Each original archive member and every selected SST byte passed independent readback. All 477 command lifetimes report exit 0 and are absent by PID/start/boot identity. The 1,492 resource samples preserve both distinct filesystem baselines and the conservative 614,907,904-byte combined peak decrease, within the original 1 GiB cap and 8 GiB floors. Packet allocation is later reporting overhead, excluded from that historical runtime peak.

Preparation contains seven passed pure metadata controls (58a9bb/0), source and binary bindings, the original metadata-only permission refusal (45093a/1), corrected preflight (26020b/0), and the exact executed helper. The separately frozen checkpoint-testing binary and OWNED-GATE.md are design inputs only: no deterministic pre-upload crash was executed. The gate also requires explicit handling of a legitimate competing predecessor after leader loss. No power-loss/object-store durability, GC eligibility, all-voter fault matrix or performance claim follows from this single-host cell.

The fixed performance-client rebuild mismatch remains an explicit limitation. Rebuilt SHA 1b8060eb168610328c10a27480de16bd8b2d6805166d8169638f85ceef0992c4 is not historical SHA 8da9af469f962a938027d1970141bbe4622f7d42b2b795f720e288fb3f8d5957 and was not substituted into any benchmark.

selection.json maps each portable member to its exact original absolute path, size and SHA256. evidence.tar.gz is a single bounded gzip archive; parts reconstruct it byte-for-byte. verify.py --originals verifies every selected member against both manifest and original bytes and consumes gzip EOF. Known-fixture-secret scan results cover current Chaos and retained host-MinIO credentials without retaining their values or hashes. Credentials, kubeconfig, CA material, executable/image bytes and duplicate payload stdout are excluded; identity pins remain. Full protocol payloads are included once under their named original paths, not silently omitted.
