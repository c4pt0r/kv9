# Actual upper-bound observer evidence

This packet retains the complete metadata inputs and original results of the 16-row, two-second default/diagnostic observer capture for source `e2e23cca5e70a9ea0cc241877b3b35b5b6433d27`. It is not candidate promotion or the full eight-smoke/sixteen-ten-second matched performance screen. Interpretation remains in the root-authored report.

The packet includes all 261 frozen analysis inputs, every before/after status and metric map, native reports, exact helper/protocol/source/role authorities, actual runtime/restoration/freezer/schema-2-checker results, timing and per-node lookup outputs, their actual terminals, and original preparation/control/failure lineage. The timing summarizer and schema-2 checker are unchanged. Only four new lookup-extraction arithmetic controls ran; prior checker/control suites were not replayed.

Actual runtime `45414/21ff48/0`, metadata freeze `c389bf/0`, checker `211df0/0`, timing summary `dfca11/0`, and lookup extraction `8d4bda/0` are retained in their original files. The accepted measured population contains 1,456,098 successful calls and 12,897,528 input items, 48 fresh drains and 64 exited owned lifetimes. All 16 permitted typed initialization-read discovery retries occurred outside measurement. Both roles use this candidate; these observations are not a CRC-versus-candidate speedup result. Whole-capture diagnostics include setup/warmup and other work inside the original status boundaries; durations are not exclusive CPU or per-call causal evidence.

`inventory.json` names every archive member, original absolute path, byte length and SHA-256, plus each numbered part and the complete gzip stream hash. The numbered `evidence.tar.gz.*` files form one ordinary gzip-compressed tar stream in lexical order. `verify.py` independently reads every member without extracting files. From the packet directory, `python3 -B verify.py --output /new/path/readback.json` performs portable byte verification; add `--original-inputs` only on the original host to also recheck selected original files. Absolute paths are historical execution provenance, not a portable runnable installation. Original bound checkers may require those execution paths; this packet does not rerun them.

`publication/selection.json`, `publication/input-hashes.json` and `publication/omissions.json` retain selection and exclusion authority. Exactly 13,176,171,178 bytes of retained database payload remain local, bound by the original per-row retention file hashes and lengths. ELF files, WAL payloads, source checkouts, and the separate capacity report payload are omitted. No original was deleted or modified. The earlier schema-1 wrapper failure remains a failure; none of its rows were reused in this new capture.

The first metadata selector invocation (`ca52e9/1`) mistakenly assumed the old failed attempt's matrix belonged to its published member index. Its source/error are retained. The matrix is now independently pinned as local historical metadata without that claim; corrected selection `2dd146/0` succeeded before any codec. All 261 current inputs were retained unchanged.

Publication caps are 96 MiB decoded member bytes, 16 MiB per member, 8 MiB compressed, 24 MiB newly allocated publication storage, an 8 GiB available-space floor, and 1,200 seconds. Packaging uses one standard gzip level-6 stream. The independent readback verifies member/input bytes and bounds; it does not repeat acceptance tests or change original conclusions.
