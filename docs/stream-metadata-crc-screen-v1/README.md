# Stream metadata on CRC: point-read/mixed evidence

This bundle preserves the accepted 24-cohort screen of candidate `bb13e4313c313ca910969d1f01ef862619b57906` against selected CRC `ca0002c7f8e9ee6f595efcc9f4151085ccce87cb`, using fixed v3 native/Redis clients `0be806d9671e2c50701a64aa7889c8859b7648ba`. [READOUT.md](READOUT.md) and [PER-REPEAT.md](PER-REPEAT.md) are verbatim original statistics outputs, including every repetition and separate GET/PUT populations.

The protocol covers c1/c64 point GET/PUT at 50% and 100% reads, three roles, and two complete forward/reverse ten-second repetitions. A separate twelve-cohort two-second smoke preceded timing. The 49,706,706 measured calls, issued calls and attempts all succeeded; every outcome population is retained. Original acceptance recorded 80 exited lifetimes, 48 fresh qualifying drains, 48 voter/listener bindings, 4,678 resource samples, and restoration of all three owned containers with historical namespaces preserved.

The candidate remains held without promotion or expanded qualification. C64 pure-read throughput changed +0.01036%, with opposite repeat signs; mixed throughput changed −0.45328%, regressing in both repeats. Mixed GET p99 has the same pooled bucket interval, but repeat 1 worsened from 622.592–630.783 to 630.784–638.975 microseconds. C1 pure-read throughput changed +0.06206%, also with opposite repeat signs; its pooled p99 improved by one bucket. These observations do not establish statistical significance, sustained capacity, full72 workload acceptance or a Chaos result. Mixed combined percentiles and GET-only percentiles refer to different populations; raw buckets are merged, never averaged. KV9 quorum/sync calls used tmpfs WAL, while standalone in-memory Redis has different durability semantics.

The archive includes all 24 exact report/cleanup/requested-config/resource-coverage sets; unchanged accepted audit and full input inventory; all frozen preparation inputs, original copies, finite diffs and six-driver/seventeen-auditor contracts; original build/Cargo/cache receipts; smoke/timing plans and matrices; isolation and root stage receipts; and frozen statistics code, input hashes and complete results. The first auditor contract failure, omitted argument-file hash, original failed auditor, exact one-literal correction and successful second run are retained. No predicate was weakened and no runtime had begun at that correction.

`process/` separately preserves ordinary stream and unary SIGKILL/restart recovery evidence: both complete histories, original reports/configuration/progress, fixture metadata, unchanged generic independent auditor, its input inventory and accepted witnesses. There are 370 operations: stream 171 OK / 14 unknown; unary 169 OK / 16 unknown. All 340 OK and 30 unknown operations remain in their original histories. The process audit accepted both histories, two client and five server lifetimes, and three fresh drained voters per transport. This is a single-host ordinary process fixture; it is not Chaos Mesh qualification or proof of every execution.

Timing session 35695 exited 0 (`d1c474`), audit session 83388 exited 0 (`a75d28`), and statistics completed synchronously with exit 0 (`ba0e28`), without a statistics session. Ordinary process session 75660 exited 0 (`65714c`), its synchronous audit exited 0 (`c109ba`), and smoke session 8170 exited 0 (`865ddf`). Original stage invocation/result files are included. `publication/tool-terminal-handoff.json` explicitly transcribes the parent task's actual tool receipts and binds those original records; it is not fabricated tool output.

Run `python3 verify.py` in this directory to verify bounded archive-part hashes, safe regular-file members, exact decoded size/SHA-256 and top-level aliases. `inventory.json` maps every member to its original source path, bytes and SHA-256. Every part is at most 2 MiB. The verifier checks byte integrity only; it does not repeat statistics, audits or runtime execution. Archive payloads remain verbatim, with normalized tar metadata.

This portable reporting selection does not enable a standalone full-runtime re-audit. Raw executables, WALs, binary descriptors and bulky host/process/container dumps remain local at the original paths bound by the accepted inventories. The timing audit's 636 retained files / 4,552,528,169 bytes describe its original runtime retention scope, not this bundle's size. Process data directories are likewise local-only. No runtime, tests, source-reader, audit or statistics was rerun for publication.
