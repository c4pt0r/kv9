# Slicing CRC ordinary-process acceptance evidence

Exact source e5662bbce1443e6907e3d832a7b300881796340c; original default-feature release and source/helper hashes are retained. Root runtime session 44594 exited 0 (5913a0). The unchanged independent auditor exited 0 inline (e9ef13), with the original first invocation/log/terminal retained.

| Transport | Complete calls | OK | Unknown | Contained loss calls | Contained restart calls |
|---|---:|---:|---:|---:|---:|
| tonic_stream | 183 | 169 | 14 | 35 | 32 |
| tonic_unary | 177 | 164 | 13 | 35 | 30 |

Both complete atomic histories include overlapping point and batch operations. All 27 unknown outcomes are retained; safe routing retries before uncertainty are allowed, and uncertain writes must not be replayed. Five voter incarnations and two clients exited, with no cleanup errors; each transport has three freshly drained voters. The original-directory restart, advertised listener, executable/PID/start/boot and source/build bindings passed unchanged predicates.

This is one local ordinary-WAL process recovery fixture, not performance, actual Chaos Mesh, power-loss, cross-host or whole-Raft refinement acceptance. Interval and listener/proc evidence comes from the source-bound fixture, not an independent continuous observer.

original-path-index.json binds exact original text bytes to each stored copy. Files over 128 KiB are deterministic gzip with both original and compressed size/hash; decompression was checked byte-for-byte. Full history JSONL, configs/reports, source/build/Cargo manifests, checker outputs, logs, process/drain/lifetime records and original command/terminal receipts are included. Native executables, fixture root.bin and every database/WAL payload are excluded and remain retained at their original paths; their hashes remain in the accepted original-input inventory. This selection does not recreate or rewrite build manifests, history or retention evidence. The preparatory wrong runner.py path read is retained in preparation-notes.json; the correct process-runner.py was used for the only actual runtime.
