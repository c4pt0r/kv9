The controlled representation gate passed on its first execution: root tool session 99479, exit 0, receipt ee4b1f; runner PID 1202665 is absent. All 14 expected cases passed without modifying the measured Rust source, the typed model/proof, or the copied semantic auditors.

There are 13 distinct parameterized theorems and 122 fresh obligations per positive TLAPS run. Four independent positive phases discharged 488 obligations in total; these are repeated checks of the same theorems. Capacity 1 explored 37 distinct states (667 generated), and capacity 2 explored 451 (8,119 generated), each with an empty final queue. The remaining six TLC cases are two complete baseline/mutant/restored triples.

The certificate mutant violated AROrdering with arOrdered=TRUE and two equal indexes after two pushes. Its concrete trace repeats an identical payload: this control establishes failure of the strict-order certificate invariant, not an observed wrong lookup result. The wrong-end eviction mutant violated ARRefinement after three pushes; the vector retained a fenceRejected receipt where the mutant deque retained plain. Thus that control demonstrates loss of the complete retained outcome, not merely a numeric index difference. Both mutants exited TLC with 12 at the named property and retained full three-/four-state traces.

The omitted-proof control was rejected as `proof hole: AppliedReceiptsProof.ARLookupAlways`; the unapproved false axiom was rejected as `unapproved module assumption: AppliedReceipts`. SANY parsed both controls, then the unchanged semantic auditor rejected them before TLAPS. Each has a fresh positive baseline and restored proof. Three retained-output controls reject empty, zero-obligation and missing-summary proof output.

The initial untyped 3/55 obligation failure and the corrected typed 122-obligation run remain included in the selection. Final readback checked all copied/current source hashes, every case source/result, all positive proof counts and semantic theorem inventories, and each intended rejection. The source-mapped conclusion covers FIFO retention, the checked ordering certificate, and identical found/absent/full index-term-outcome observations for arbitrary input sequences under the explicit positive-capacity and standard-library premises. It introduces no producer ordering or success-only assumption.

Rust 1.94 VecDeque sorted-search semantics, ordinary container operations, compiler/allocator and unchanged caller locking are trusted boundaries. ARAny is an abstract chosen match under uniqueness, not an executable model of binary search on invalid input. This is not a whole-Rust, grouped-read/Ready, Raft, liveness, cancellation or crash durability proof. Candidate 74d24116 remains experimental; process and performance evidence are separate.

The publication manifest selects exact small sources, logs, traces, semantic inventories, commands and actual terminal records. It excludes TLC state stores, proof caches, compiled Java classes, tool executables and runtime WAL/binary artifacts. It does not rerun any proof or measurement.
