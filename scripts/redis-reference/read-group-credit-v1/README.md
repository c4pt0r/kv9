# ReadIndex credit: executed screening and correctness evidence

Candidate source is `57ff6851e40ed63c837189d6eb0a11190704725a`, compared with
accepted `5ee897a` using unchanged native `03c1c776` and Redis `b8ec38f`
measurement clients. The first complete recording and independent audit pass.
The c64 gain is retained as a candidate; c1 point GET regresses slightly and
general promotion remains open. See the [screening report](../../../docs/READ-GROUP-CREDIT-SCREENING.md)
and [complete readout](READOUT.md).

`inventory.json` records source paths, lengths and SHA-256 hashes for exact
copies. This authored README and the inventory itself are excluded. Original
raw evidence remains at those absolute paths; this directory is not a portable
replacement for the pinned source checkouts, releases and local toolchains.
`preparation-readme.md` preserves the exact smoke, timing and audit commands.
Keep `/usr/bin/redis-server` as its lexical invocation path: resolving the
multicall symlink changes its behavior.

The six driver and ten auditor contracts pass with final pins. The retained
helper delta changes candidate paths, protocol/revision and build identities;
workload, validation and restoration predicates remain unchanged. The unified
diff's intentional blank context-line prefixes have an exact-path whitespace
attribute so its original bytes are preserved. Earlier unfinished preparation
is retained under the original preparation's `before-final-pins` directory.

`source-gates.json`, `source-controls.json` and `workspace-gates.json` bind the
218 Raft tests, 14 compiled semantic-control triples, 714 default workspace
tests and Clippy. Source review preserves two failed audit attempts: the first
reader omitted Tokio tests; the second encountered root's commit while
requiring an unchanged HEAD. Parser/commit-binding corrections preserved all
source-content requirements. Historical mutant executables can share a private
target path; source/log/Cargo/hash evidence is retained, while only the final
restored executable remains independently rehashable at that path.

`formal-summary.json` and independent proof review record 37 accepted cases,
28 parameterized theorems and 265 freshly checked obligations. Earlier wrapper
failures remain in `formal-summary-first.json` and `formal-summary-second.json`.
They were corrected without lowering the original proof/counterexample
requirements. The source design also retains the initial proof-draft failure.
The formal result concerns a single-invocation admission projection and
conditional progress, not complete grouped-read/Ready/Rust refinement.

`process-audit.json` records 353 complete operations (324 OK, 29 unknown) with
leader loss and original-directory restart for both streaming and unary RPC.
All owned process lifetimes exit. This is one-host process recovery evidence;
exact-source Chaos Mesh acceptance and power-loss semantics are separate gates.

The timing protocol uses 24 five-second read-only cohorts: c1/c64, GET and
BatchGet(1), accepted/candidate sources, Redis GET/MGET(1), two reversed repeats.
All 28,847,557 issued logical calls succeed with no measured retry, non-success
outcome or dropped slot. The 12-cell correctness smoke is excluded from timing.
The audit verifies complete histograms, source/build identity, process cleanup,
fresh drains, retained storage data and exact CPU-mask restoration.

The separate endpoint readout preserves the prior arithmetic and directly
hashes before/after status snapshots with accepted voter start-tick bindings.
Its first adapter's false assumption that both snapshots appeared in the main
auditor inventory is preserved as a failure. The corrected result makes no
such claim: it covers full setup/warmup/measurement/verification envelopes and
does not change the accepted timing audit or its performance statistics.

This is a shared-host three-voter volatile-tmpfs-WAL diagnostic against
standalone Redis memory operation with persistence disabled and no pipeline.
It does not establish equivalent durability, sustained capacity or Redis parity.
No build, test, fault, profile or independent audit overlaps timing. No hosted
CI runs. The original source, formal, smoke, process and measurement recordings
remain available at the paths in the retained evidence.
