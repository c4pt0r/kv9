# Exact read-group-credit Chaos acceptance

Candidate 57ff6851e40ed63c837189d6eb0a11190704725a; existing default release only. No Rust rebuild. Parent delegated task authorizes this one run after source/image/configuration checks.

The accepted 5ee runner, protocol, observers, validators, package tools and leaf readback are unchanged. Only source/build/image/owned-path pins and image probe names changed. The eleven original windows and all negative-outcome, identity, drain, effect and cleanup requirements remain mandatory. Any failure is retained and does not authorize a rerun.

Command: env PYTHONDONTWRITEBYTECODE=1 PYTHONOPTIMIZE=0 taskset -c 6-15,22-31 /usr/bin/python3 /tmp/kv9-read-group-credit-chaos-preparation-first/run.py --plan /tmp/kv9-read-group-credit-chaos-preparation-first/plan.json --release /tmp/kv9-read-group-credit-chaos-preparation-first/launch-release.json --artifact /tmp/kv9-native-link-acceptance-57ff685-attempt1

After fixture terminal: unchanged audit.py and leaf_readback.py on retained raw artifacts, then source/frozen input integrity. All work is correctness-only on one Kind host; reset is non-Chaos SOCK_DESTROY. Report only exact netem leaf drops, not legacy parent+child sum.
