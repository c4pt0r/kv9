# WorkSignal screen and point-write measurement evidence

The [completed report](../../../docs/WORK-SIGNAL-SCREENING.md) does not select
`1b70dba6` as a general performance increment. The [full readout](READOUT.md)
retains every matched pair and Redis control. The [Redis execution analysis](../../../docs/REDIS-EXECUTION-PATH.md)
connects the result to measured coordination costs and the next workload scope.

`PREPARATION.md`, the frozen driver/protocol/auditor, their contracts, role
preflight and `results-first/` retain the first successful 48-cohort recording's
independent acceptance. `outer/` retains invocation, isolation and restoration
summary. Full raw records remain under
`/tmp/kv9-work-signal-matched-diagnostic-attempt1`; executables and imported
historical helpers remain at their pinned paths. This directory is an evidence
publication, not a relocatable runtime bundle. Container inspection documents
and raw binaries are not copied into the repository.

`next-client/` records the separate `0be806d9` v3 measurement-client increment:
70 focused Rust/Python tests, format and Clippy checks, two clean default-release
builds, the preserved first preflight refusal, frozen real-runtime preparation,
and the first twelve-case correctness smoke. Redis's operation-cap termination
remains explicitly timing-ineligible; no smoke numbers select performance.
Final deterministic scans do not reconstruct exact issued nonces or establish
full-history linearizability. The original preparation draft and the independent
reviewer's cutoff correction are retained.

`exact-copy-inventory.json` maps every copied file to its original absolute path,
byte count and SHA-256. It excludes itself and this authored index. Retained
diffs and logs preserve their original whitespace. GitHub CI is not dispatched.
