# Inbox-vector screening and next notification candidate

See [the development readout](../../../docs/RAFT-INBOX-DRAIN-SCREENING.md).
This directory retains exact evidence copies, not a portable replacement for
the original source checkouts, releases and raw recording.

- [READOUT.md](READOUT.md) and [statistics-first.json](statistics-first.json)
  retain all sixteen old/new QPS/mean/p99 pairs, all sixteen Redis controls,
  every outcome population and correctly weighted pooled values.
- [PREPARATION.md](PREPARATION.md), [protocol.json](protocol.json), driver,
  independent auditor, both contract files, complete diffs and frozen inputs
  record the study declared before timing. The preparation README is copied
  unchanged under its explicit retained name.
- [results-first/audit.json](results-first/audit.json) and its input inventory
  accept all 48 cohorts, 117,886,444 successful calls and exact process/resource/
  cleanup/restoration bindings. Root timing 98766 and audit 4119 exit 0.
  The separate 12-cell smoke, root 92541, is excluded from performance.
- `outer/` contains the original timing invocation, isolation and final
  restoration summary. Full raw files remain at
  `/tmp/kv9-inbox-drain-matched-diagnostic-attempt1`.
- `freeze-first-failure.json` preserves the root smoke-summary adapter's
  incorrect cleanup-field assumption; `AUDITOR-PREPARATION.initial.json`
  preserves the corrected metadata draft. Runtime/helpers were not rerun or
  weakened to erase either preparation correction.
- `next-source/` records the separate `1b70dba6` WorkSignal notification
  candidate: 219-test/Clippy source gate, actual-delivery negative control and
  exact restoration, independent local source argument and default release.
  Its independently checked process E2E passes 351 calls, all four progress
  windows, six fresh drains and seven exited lifetimes. Process audit SHA-256:
  `30bc6a1f9702f8ba3ebb1174ec72d0785b49b68d30a441b634efcd0cf1b1afbd`.
  It starts from `57ff6851`, without the inbox-vector change. These records
  contain no measured performance gain or candidate-specific Chaos acceptance.

[copy-inventory.json](copy-inventory.json) binds every copied file to its
original path, hash and size. It excludes itself and this newly written index.
Absolute paths in the original helpers, logs and manifests remain unchanged;
raw workloads and binaries are not duplicated. Path-specific Git attributes
preserve exact unified-diff context and test-log endings.

The completed inbox-vector screen does not justify selecting `c3131800` as
the next performance increment: pooled improvements are small and individual
throughput/p99 directions are mixed. All observations remain retained. The
general baseline is still `5ee897a`; Redis parity, broader load/fault coverage
and whole-protocol formal composition remain open. No hosted CI runs.
